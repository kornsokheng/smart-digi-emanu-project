# Optional: Google Apps Script (or other) URL for order JSON after successful KHQR payment
# VITE_LUMHO_ORDER_WEBHOOK=https://script.google.com/macros/s/.../exec

# Optional: hosted backend API URL, for example https://your-api.onrender.com
# Leave empty when frontend and API are served from the same domain.
# VITE_API_BASE_URL=
